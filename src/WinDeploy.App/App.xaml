<Application x:Class="WinDeploy.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:WinDeploy.App.ViewModels"
             xmlns:vmd="clr-namespace:WinDeploy.App.ViewModels.Deploy"
             xmlns:vmsys="clr-namespace:WinDeploy.App.ViewModels.Sys"
             xmlns:vmftp="clr-namespace:WinDeploy.App.ViewModels.Ftp"
             xmlns:vmnet="clr-namespace:WinDeploy.App.ViewModels.Net"
             xmlns:vmterm="clr-namespace:WinDeploy.App.ViewModels.Terminal"
             xmlns:vmtools="clr-namespace:WinDeploy.App.ViewModels.Tools"
             xmlns:vmshell="clr-namespace:WinDeploy.App.ViewModels.Shell"
             xmlns:vd="clr-namespace:WinDeploy.App.Views.Deploy"
             xmlns:vsys="clr-namespace:WinDeploy.App.Views.Sys"
             xmlns:vftp="clr-namespace:WinDeploy.App.Views.Ftp"
             xmlns:vcf="clr-namespace:WinDeploy.App.Views.Cloudflare"
             xmlns:vsrv="clr-namespace:WinDeploy.App.Views.Server"
             xmlns:vterm="clr-namespace:WinDeploy.App.Views.Terminal"
             xmlns:vtools="clr-namespace:WinDeploy.App.Views.Tools"
             xmlns:vshell="clr-namespace:WinDeploy.App.Views.Shell"
             xmlns:app="clr-namespace:WinDeploy.App"
             xmlns:b="clr-namespace:WinDeploy.App.Behaviors"
             StartupUri="MainWindow.xaml">
  <Application.Resources>
    <ResourceDictionary>

      <BooleanToVisibilityConverter x:Key="BoolToVis" />
      <app:ZeroToVisibilityConverter x:Key="ZeroToVis" />

      <!-- Palette — replaced wholesale by ThemeManager on theme change; all usages are DynamicResource -->
      <SolidColorBrush x:Key="PageBg" Color="#F5F5F2" />
      <SolidColorBrush x:Key="CardBg" Color="#FFFFFF" />
      <SolidColorBrush x:Key="BorderSoft" Color="#E6E6E1" />
      <SolidColorBrush x:Key="BorderStrong" Color="#D6D6D0" />
      <SolidColorBrush x:Key="TextPrimary" Color="#1B1B1A" />
      <SolidColorBrush x:Key="TextSecondary" Color="#6E6E69" />
      <SolidColorBrush x:Key="TextTertiary" Color="#9A9A93" />
      <SolidColorBrush x:Key="Accent" Color="#185FA5" />
      <SolidColorBrush x:Key="AccentBg" Color="#E6F1FB" />
      <SolidColorBrush x:Key="OkFg" Color="#3B6D11" />
      <SolidColorBrush x:Key="OkBg" Color="#EAF3DE" />
      <SolidColorBrush x:Key="FailFg" Color="#A32D2D" />
      <SolidColorBrush x:Key="FailBg" Color="#FCEBEB" />
      <SolidColorBrush x:Key="WarnFg" Color="#9A5B00" />
      <SolidColorBrush x:Key="WarnBg" Color="#FBEFDD" />
      <SolidColorBrush x:Key="NavBg" Color="#FBFBF9" />

      <!-- Base text -->
      <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
      </Style>

      <!-- Card -->
      <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CardBg}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderSoft}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="10" />
      </Style>

      <!-- Primary button -->
      <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Foreground" Value="{DynamicResource Accent}" />
        <Setter Property="Background" Value="{DynamicResource AccentBg}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Accent}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="14,7" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Button">
              <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
              </Border>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
        <Style.Triggers>
          <Trigger Property="IsEnabled" Value="False">
            <Setter Property="Opacity" Value="0.45" />
          </Trigger>
        </Style.Triggers>
      </Style>

      <!-- Small secondary button -->
      <Style x:Key="MiniButton" TargetType="Button">
        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}" />
        <Setter Property="Background" Value="{DynamicResource CardBg}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderSoft}" />
        <Setter Property="Padding" Value="11,5" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Button">
              <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="1" CornerRadius="6" Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
              </Border>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
        <Style.Triggers>
          <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.4" /></Trigger>
        </Style.Triggers>
      </Style>

      <!-- Small round icon button on cards (e.g. ▶ 启动) -->
      <Style x:Key="CardIconButton" TargetType="Button">
        <Setter Property="Width" Value="22" />
        <Setter Property="Height" Value="22" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Foreground" Value="{DynamicResource Accent}" />
        <Setter Property="Background" Value="{DynamicResource AccentBg}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Button">
              <Border x:Name="B" Background="{TemplateBinding Background}" CornerRadius="11">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="B" Property="Background" Value="{DynamicResource Accent}" />
                  <Setter Property="Foreground" Value="{DynamicResource CardBg}" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!-- Tooltips (theme-aware) -->
      <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{DynamicResource CardBg}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderStrong}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="9,6" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ToolTip">
              <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                <ContentPresenter />
              </Border>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!-- Small round STOP button on cards (■) -->
      <Style x:Key="CardStopButton" TargetType="Button">
        <Setter Property="Width" Value="22" />
        <Setter Property="Height" Value="22" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="Foreground" Value="{DynamicResource FailFg}" />
        <Setter Property="Background" Value="{DynamicResource FailBg}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Button">
              <Border x:Name="B" Background="{TemplateBinding Background}" CornerRadius="11">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="B" Property="Background" Value="{DynamicResource FailFg}" />
                  <Setter Property="Foreground" Value="{DynamicResource CardBg}" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!-- Inputs ─────────────────────────────────────────────────────────── -->
      <Style TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource CardBg}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderStrong}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CaretBrush" Value="{DynamicResource TextPrimary}" />
        <Setter Property="SelectionBrush" Value="{DynamicResource Accent}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="TextBox">
              <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}"
                              VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                              VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}"
                              HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                  <Setter Property="BorderBrush" Value="{DynamicResource Accent}" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!-- Dropdown (non-editable) -->
      <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="9,6" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ComboBoxItem">
              <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="4">
                <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                  <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBg}" />
                  <Setter Property="Foreground" Value="{DynamicResource Accent}" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
        <Setter Property="Background" Value="{DynamicResource CardBg}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderStrong}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="9,0" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ComboBox">
              <Grid>
                <ToggleButton Focusable="False" ClickMode="Press"
                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                  <ToggleButton.Template>
                    <ControlTemplate TargetType="ToggleButton">
                      <Border Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderStrong}"
                              BorderThickness="1" CornerRadius="6">
                        <Path HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"
                              Data="M0,0 L4,4 L8,0 Z" Fill="{DynamicResource TextSecondary}" />
                      </Border>
                    </ControlTemplate>
                  </ToggleButton.Template>
                </ToggleButton>
                <ContentPresenter IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}"
                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                  TextElement.Foreground="{DynamicResource TextPrimary}"
                                  Margin="{TemplateBinding Padding}" VerticalAlignment="Center" HorizontalAlignment="Left" />
                <Popup x:Name="PART_Popup" Placement="Bottom" AllowsTransparency="True" Focusable="False"
                       IsOpen="{TemplateBinding IsDropDownOpen}" PopupAnimation="Slide">
                  <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                          MaxHeight="{TemplateBinding MaxDropDownHeight}" Margin="0,2,0,6"
                          Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderStrong}"
                          BorderThickness="1" CornerRadius="6">
                    <ScrollViewer>
                      <ItemsPresenter />
                    </ScrollViewer>
                  </Border>
                </Popup>
              </Grid>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!-- Selectors -->
      <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
      </Style>
      <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
      </Style>

      <!-- DataGrid (environment variables) -->
      <Style TargetType="DataGrid">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="RowBackground" Value="Transparent" />
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource BorderSoft}" />
        <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource BorderSoft}" />
        <Setter Property="RowHeaderWidth" Value="0" />
      </Style>
      <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{DynamicResource PageBg}" />
        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderSoft}" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
      </Style>
      <Style TargetType="DataGridCell">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="DataGridCell">
              <Border Background="{TemplateBinding Background}" Padding="4,3">
                <ContentPresenter VerticalAlignment="Center" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsSelected" Value="True">
                  <Setter Property="Background" Value="{DynamicResource AccentBg}" />
                  <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!-- Scrollbars (all pages) ─────────────────────────────────────────── -->
      <Style x:Key="WdScrollThumb" TargetType="Thumb">
        <Setter Property="Background" Value="{DynamicResource BorderStrong}" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Thumb">
              <Border CornerRadius="4" Background="{TemplateBinding Background}" Margin="2" />
            </ControlTemplate>
          </Setter.Value>
        </Setter>
        <Style.Triggers>
          <Trigger Property="IsMouseOver" Value="True">
            <Setter Property="Background" Value="{DynamicResource TextTertiary}" />
          </Trigger>
        </Style.Triggers>
      </Style>
      <Style x:Key="WdScrollPage" TargetType="RepeatButton">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="RepeatButton">
              <Border Background="Transparent" />
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Width" Value="11" />
        <Setter Property="MinWidth" Value="11" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ScrollBar">
              <Grid Background="{TemplateBinding Background}">
                <Track x:Name="PART_Track" IsDirectionReversed="True">
                  <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource WdScrollPage}" Command="ScrollBar.PageUpCommand" />
                  </Track.DecreaseRepeatButton>
                  <Track.Thumb>
                    <Thumb Style="{StaticResource WdScrollThumb}" />
                  </Track.Thumb>
                  <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource WdScrollPage}" Command="ScrollBar.PageDownCommand" />
                  </Track.IncreaseRepeatButton>
                </Track>
              </Grid>
              <ControlTemplate.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                  <Setter Property="Width" Value="Auto" />
                  <Setter Property="MinWidth" Value="0" />
                  <Setter Property="Height" Value="11" />
                  <Setter Property="MinHeight" Value="11" />
                  <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!-- Semantic colored buttons (启动=绿 / 结束=红 / 重启=蓝) -->
      <Style x:Key="OkButton" TargetType="Button" BasedOn="{StaticResource MiniButton}">
        <Setter Property="Foreground" Value="{DynamicResource OkFg}" />
        <Setter Property="Background" Value="{DynamicResource OkBg}" />
        <Setter Property="BorderBrush" Value="{DynamicResource OkFg}" />
      </Style>
      <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource MiniButton}">
        <Setter Property="Foreground" Value="{DynamicResource FailFg}" />
        <Setter Property="Background" Value="{DynamicResource FailBg}" />
        <Setter Property="BorderBrush" Value="{DynamicResource FailFg}" />
      </Style>
      <Style x:Key="InfoButton" TargetType="Button" BasedOn="{StaticResource MiniButton}">
        <Setter Property="Foreground" Value="{DynamicResource Accent}" />
        <Setter Property="Background" Value="{DynamicResource AccentBg}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Accent}" />
      </Style>
      <Style x:Key="WarnButton" TargetType="Button" BasedOn="{StaticResource MiniButton}">
        <Setter Property="Foreground" Value="{DynamicResource WarnFg}" />
        <Setter Property="Background" Value="{DynamicResource WarnBg}" />
        <Setter Property="BorderBrush" Value="{DynamicResource WarnFg}" />
      </Style>

      <!-- Dropdown toggle button (looks like MiniButton; checked = accent) -->
      <Style x:Key="MiniToggle" TargetType="ToggleButton">
        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}" />
        <Setter Property="Background" Value="{DynamicResource CardBg}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderSoft}" />
        <Setter Property="Padding" Value="11,5" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ToggleButton">
              <Border x:Name="B" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="1" CornerRadius="6" Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True">
                  <Setter TargetName="B" Property="Background" Value="{DynamicResource AccentBg}" />
                  <Setter TargetName="B" Property="BorderBrush" Value="{DynamicResource Accent}" />
                  <Setter Property="Foreground" Value="{DynamicResource Accent}" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!-- Filter chip (toggle) — theme-aware -->
      <Style x:Key="ChipToggle" TargetType="ToggleButton">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Margin" Value="0,0,6,4" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ToggleButton">
              <Border x:Name="B" CornerRadius="11" Padding="10,3" BorderThickness="1"
                      Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BorderSoft}">
                <TextBlock x:Name="T" Text="{TemplateBinding Content}" FontSize="11.5" Foreground="{DynamicResource TextSecondary}" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True">
                  <Setter TargetName="B" Property="Background" Value="{DynamicResource AccentBg}" />
                  <Setter TargetName="B" Property="BorderBrush" Value="{DynamicResource Accent}" />
                  <Setter TargetName="T" Property="Foreground" Value="{DynamicResource Accent}" />
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="B" Property="BorderBrush" Value="{DynamicResource Accent}" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!-- Slider (terminal FX opacity / speed) -->
      <Style TargetType="Slider">
        <Setter Property="MinHeight" Value="22" />
        <Setter Property="Cursor" Value="Hand" />
        <!-- Click the track to jump the thumb to that point (instead of a full-range page step). -->
        <Setter Property="IsMoveToPointEnabled" Value="True" />
        <Setter Property="SmallChange" Value="0.05" />
        <Setter Property="LargeChange" Value="0.1" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Slider">
              <Grid VerticalAlignment="Center" Height="22">
                <Border Height="4" CornerRadius="2" VerticalAlignment="Center" Background="{DynamicResource BorderSoft}" />
                <Track x:Name="PART_Track">
                  <Track.DecreaseRepeatButton>
                    <RepeatButton Focusable="False" Command="Slider.DecreaseLarge">
                      <RepeatButton.Template>
                        <ControlTemplate TargetType="RepeatButton">
                          <Border Height="4" CornerRadius="2" VerticalAlignment="Center" Background="{DynamicResource Accent}" />
                        </ControlTemplate>
                      </RepeatButton.Template>
                    </RepeatButton>
                  </Track.DecreaseRepeatButton>
                  <Track.IncreaseRepeatButton>
                    <RepeatButton Focusable="False" Command="Slider.IncreaseLarge">
                      <RepeatButton.Template>
                        <ControlTemplate TargetType="RepeatButton">
                          <Border Background="Transparent" />
                        </ControlTemplate>
                      </RepeatButton.Template>
                    </RepeatButton>
                  </Track.IncreaseRepeatButton>
                  <Track.Thumb>
                    <Thumb>
                      <Thumb.Template>
                        <ControlTemplate TargetType="Thumb">
                          <Ellipse Width="16" Height="16" Fill="{DynamicResource Accent}" />
                        </ControlTemplate>
                      </Thumb.Template>
                    </Thumb>
                  </Track.Thumb>
                </Track>
              </Grid>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!-- Themed context menu (matches the app theme instead of the OS default) -->
      <Style x:Key="ThemedContextMenu" TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource CardBg}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderStrong}" />
        <Setter Property="HasDropShadow" Value="False" />
        <!-- Auto-hide leading/trailing/consecutive separators left behind by conditionally-hidden items. -->
        <Setter Property="b:MenuTidy.AutoSeparators" Value="True" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ContextMenu">
              <!-- No DropShadowEffect: on a non-transparent popup it renders as a black halo in light mode. -->
              <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="1" CornerRadius="8" Padding="4">
                <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
              </Border>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <Style x:Key="ThemedMenuItem" TargetType="MenuItem">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="MenuItem">
              <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6"
                      Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="22" />
                    <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>
                  <ContentPresenter Grid.Column="0" ContentSource="Icon" VerticalAlignment="Center" HorizontalAlignment="Center" />
                  <ContentPresenter Grid.Column="1" ContentSource="Header" VerticalAlignment="Center" Margin="8,0,4,0"
                                    RecognizesAccessKey="True" TextElement.Foreground="{TemplateBinding Foreground}" />
                </Grid>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                  <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBg}" />
                  <Setter Property="Foreground" Value="{DynamicResource Accent}" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter Property="Foreground" Value="{DynamicResource TextTertiary}" />
                  <Setter TargetName="Bd" Property="Opacity" Value="0.4" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <Style x:Key="ThemedSeparator" TargetType="Separator">
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Separator">
              <Border Height="1" Background="{DynamicResource BorderSoft}" Margin="8,4" />
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!-- Page → View map (feature sub-namespaces: vmX = ViewModels.X, vX = Views.X) -->
      <DataTemplate DataType="{x:Type vmd:InstallCenterViewModel}">
        <vd:InstallCenterView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmd:ProgressViewModel}">
        <vd:ProgressView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmd:ConfigSyncViewModel}">
        <vd:ConfigSyncView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmd:ExportViewModel}">
        <vd:ExportView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmd:DetailViewModel}">
        <vd:DetailView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmshell:SettingsViewModel}">
        <vshell:SettingsView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmshell:LogViewModel}">
        <vshell:LogView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmsys:EnvVarsViewModel}">
        <vsys:EnvVarsView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmsys:ProcessManagerViewModel}">
        <vsys:ProcessManagerView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmsys:StartupViewModel}">
        <vsys:StartupView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmsys:SystemOverviewViewModel}">
        <vsys:SystemOverviewView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmsys:PowerViewModel}">
        <vsys:PowerView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmsys:MaintenanceViewModel}">
        <vsys:MaintenanceView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmterm:TerminalViewModel}">
        <vterm:TerminalView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmtools:WslViewModel}">
        <vtools:WslView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmtools:TweaksViewModel}">
        <vtools:TweaksView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmtools:AdvancedToolsViewModel}">
        <vtools:AdvancedToolsView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmnet:ServiceConfigViewModel}">
        <vsrv:ServiceConfigView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmnet:ServerListViewModel}">
        <vsrv:ServerListView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmnet:ServerDetailViewModel}">
        <vsrv:ServerDetailView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmftp:FtpViewModel}">
        <vftp:FtpView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmftp:FtpServerViewModel}">
        <vftp:FtpServerView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmftp:FtpConfigViewModel}">
        <vftp:FtpConfigView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmftp:FtpClientViewModel}">
        <vftp:FtpClientView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vmnet:CloudflareDdnsViewModel}">
        <vcf:CloudflareDdnsView />
      </DataTemplate>
      <DataTemplate DataType="{x:Type vm:PlaceholderViewModel}">
        <vshell:PlaceholderView />
      </DataTemplate>

    </ResourceDictionary>
  </Application.Resources>
</Application>
