<UserControl x:Class="WinDeploy.App.Views.Shell.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="24,18,24,18">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0">
      <TextBlock Text="{DynamicResource S.log.title}" FontSize="20" FontWeight="SemiBold" />
      <TextBlock Text="{DynamicResource S.log.subtitle}" FontSize="13"
                 Foreground="{DynamicResource TextSecondary}" Margin="0,3,0,0" />
    </StackPanel>

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0">
      <Button Content="{DynamicResource S.common.refresh}" Style="{StaticResource MiniButton}" Command="{Binding RefreshCommand}" />
      <Button Content="{DynamicResource S.log.openFolder}" Style="{StaticResource MiniButton}" Command="{Binding OpenFolderCommand}" Margin="8,0,0,0" />
      <Button Content="{DynamicResource S.log.clear}" Style="{StaticResource MiniButton}" Command="{Binding ClearCommand}" Margin="8,0,0,0"
              ToolTip="{DynamicResource S.log.clear.tip}" />
      <TextBlock Text="{Binding FilePath}" FontSize="11" Foreground="{DynamicResource TextTertiary}"
                 VerticalAlignment="Center" Margin="16,0,0,0" TextTrimming="CharacterEllipsis" />
    </StackPanel>

    <Border Grid.Row="2" Style="{StaticResource Card}" Padding="2" Margin="0,12,0,0">
      <TextBox x:Name="OutBox" Text="{Binding Text, Mode=OneWay}" IsReadOnly="True"
               FontFamily="Consolas" FontSize="12.5" TextWrapping="NoWrap" BorderThickness="0"
               Background="Transparent" VerticalContentAlignment="Top" Padding="10"
               VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
               TextChanged="OutBox_TextChanged" />
    </Border>
  </Grid>
</UserControl>
